# Copy to .env and adjust as needed, then:  export $(grep -v '^#' .env | xargs)

# --- Connection ---
PGHOST=localhost
PGPORT=5432
PGDATABASE=salesdw
PGUSER=wh
PGPASSWORD=wh

# --- Data scale (override for the big final run) ---
N_ITEMS=2000
N_CUSTOMERS=20000
N_STORES=200
N_FACTS=500000
